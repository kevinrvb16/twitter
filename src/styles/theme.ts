export interface ITheme {
    color: {
        blue: string
        gray: string
        darkGray: string
        borderColor: string
    }
}
export const theme: ITheme = {
    color: {
        blue: '#1DA1F2',
        gray: '#8899A6',
        darkGray: '#253343',
        borderColor: '#2F3336'
    }
}